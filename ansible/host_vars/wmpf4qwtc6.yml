# host_vars for 'wmpf4qwtc6' (group: modem, ip: 192.168.1.25)
# These override ansible/group_vars/modem.yml for this host only.
# Uncomment and set whatever differs for this specific machine:
#
# ansible_user: someuser
# ansible_ssh_private_key_file: ~/.ssh/id_rsa
# ansible_port: 22
