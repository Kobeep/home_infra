all:
  children:
    pc:
      hosts:
        KobeePC:
          ansible_host: localhost
        KingaPC:
          ansible_host:
    server:
      hosts:
        homelab:
          ansible_host: 192.168.1.16
          ansible_user: server
          ansible_ssh_private_key_file: ~/.ssh/server/id_rsa
