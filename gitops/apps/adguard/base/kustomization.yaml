apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: adguard
resources:
  - namespace.yaml
  - pvc-conf.yaml
  - pvc-work.yaml
  - deployment.yaml
  - service.yaml
commonLabels:
  app: adguard
