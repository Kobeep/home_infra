apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins
  namespace: jenkins
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: jenkins
    spec:
      serviceAccountName: jenkins-admin
      securityContext:
        fsGroup: 1000
        runAsUser: 1000
      containers:
      - name: jenkins
        image: ghcr.io/kobeep/jenkins_custom:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 50000
          name: agent
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "1000m"
            memory: "1024Mi"
        volumeMounts:
        - name: jenkins-data
          mountPath: /var/jenkins_home
        livenessProbe:
          httpGet:
            path: /login
            port: 8080
          initialDelaySeconds: 120
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /login
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
        env:
        - name: JAVA_OPTS
          value: "-Djenkins.install.runSetupWizard=false"
        - name: REF_PLUGINS_FILE
          value: "/usr/share/jenkins/ref/plugins.txt"
        - name: KOBEEP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: jenkins-secrets
              key: kobeep-password
        - name: HARBOR_MY_USERNAME
          valueFrom:
            secretKeyRef:
              name: jenkins-secrets
              key: harbor-my-username
              optional: true
        - name: HARBOR_MY_PASSWORD
          valueFrom:
            secretKeyRef:
              name: jenkins-secrets
              key: harbor-my-password
              optional: true
        - name: HARBOR_JENKINS_USERNAME
          valueFrom:
            secretKeyRef:
              name: jenkins-secrets
              key: harbor-jenkins-username
              optional: true
        - name: HARBOR_JENKINS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: jenkins-secrets
              key: harbor-jenkins-password
              optional: true
        - name: SERVER_SSH_USERNAME
          valueFrom:
            secretKeyRef:
              name: jenkins-secrets
              key: server-ssh-username
              optional: true
        - name: SERVER_SSH_PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              name: jenkins-secrets
              key: server-ssh-private-key
              optional: true
        - name: ANSIBLE_VAULT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: jenkins-secrets
              key: ansible-vault-password
              optional: true
      volumes:
      - name: jenkins-data
        persistentVolumeClaim:
          claimName: jenkins-data
