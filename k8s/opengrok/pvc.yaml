apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opengrok-data
  namespace: opengrok
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 5Gi
