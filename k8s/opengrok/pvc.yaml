apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: git-local-pvc
  namespace: opengrok
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 1Gi
