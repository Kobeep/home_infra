# AdGuard Home Configuration

This directory contains backed up AdGuard Home configurations.

## Structure
- `work/` - AdGuard Home working directory (/opt/adguardhome/work)
- `conf/` - AdGuard Home configuration directory (/opt/adguardhome/conf)

Configuration files are automatically backed up and restored by Jenkins pipelines.