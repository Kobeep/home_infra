# OpenWebUI Configuration

This directory contains backed up OpenWebUI configurations and data.

## Structure
- Contains data from `/app/backend/data` path in OpenWebUI container
- Deployed in the `ai` namespace

Configuration files are automatically backed up and restored by Jenkins pipelines.